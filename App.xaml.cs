using System.Windows;

namespace ExcelToSqlImporter;

/// <summary>
/// WPF Application entry point.
/// InitializeComponent() is called via the generated partial class from App.xaml.
/// </summary>
public partial class App : Application
{
    public App()
    {
        InitializeComponent();
    }
}
